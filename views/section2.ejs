<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Section 2</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #212529;
        color: white;
        padding: 20px;
      }
      h1 {
        color: #fff;
        margin-bottom: 20px;
      }
      label {
        color: white;
      }
      input[type="text"],
      input[type="number"] {
        background-color: #454d55;
        color: white;
        border: 1px solid #6c757d;
        padding: 5px 10px;
        margin-bottom: 10px;
      }
      button[type="submit"] {
        background-color: #007bff;
        border-color: #007bff;
        color: white;
        padding: 5px 10px;
        cursor: pointer;
      }
      button[type="submit"]:hover {
        background-color: #0056b3;
        border-color: #0056b3;
      }
      p {
        margin-top: 20px;
        font-size: 18px;
      }
    </style>
  </head>
  <body>
    <h1>Section 2: Khoj the Search</h1>
    <form action="/section2/khoj" method="post">
      <label for="inputValues">Input Values (comma-separated integers):</label>
      <input type="text" id="inputValues" name="inputValues" <% if (typeof
      inputValues !== "undefined") { %> value="<%= inputValues %>" <% } %>
      required />
      <br />
      <label for="searchValue">Search Value:</label>
      <input type="number" id="searchValue" name="searchValue" <% if (typeof
      searchValue !== "undefined") { %> value="<%= searchValue %>" <% } %>
      required />
      <br />
      <button type="submit">Khoj</button>
    </form>

    <% if (typeof isSearchValueInInput !== "undefined") { %>
    <p>Result: <%= isSearchValueInInput ? "True" : "False" %></p>
    <% } %>
  </body>
</html>
